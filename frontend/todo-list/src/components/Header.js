import React from 'react'

const Header = () => {
    return (
        <header className='flex items-center justify-center text-white bg-blue-500'>
            <h1>Todo List</h1>
        </header>
    );
};

export default Header
